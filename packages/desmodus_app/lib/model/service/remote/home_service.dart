/*
import 'package:desmodus_app/model/local/entity/article.dart';
// import 'package:http/http.dart' as http;
// import 'dart:convert';

class HomeService {
  Future<List<Article>> obtenerReportesRecientes() async {
    // Simula una llamada de red con 1 segundo de delay
    await Future.delayed(const Duration(seconds: 1), () {});

    return mockReportes;

    // // TODO: implementar REST API real cuando esté disponible
    // final res = await http.get(Uri.parse("${Config.apiUrl}/ruta-de-reportes"));
    //
    // if (res.statusCode == 200) {
    //   final List<dynamic> data = json.decode(res.body);
    //   return data.map((report) => Article.fromJson(report)).toList();
    // } else {
    //   throw Exception('Error al cargar reportes');
    // }
  }
}
*/
